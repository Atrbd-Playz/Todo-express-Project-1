<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Manager | Backend Project-1</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-800">
    <h1 class="text-white text-3xl px-8 py-5 m-3 font-sans">Task Manager | Backend Project-1</h1>
    <div class="main w-full">
        <div class="form flex flex-col p-8 m-6 text-gray-50">
            <form action="/create" method="post">


                <input type="text" name="title" placeholder="Type Task title here.."
                    class="bg-zinc-700 w-[100%] p-2 rounded-md focus:outline-none" />


                <textarea name="details" placeholder="Type Task Details here.."
                    class="bg-zinc-700 w-[100%] resize-none mt-4 p-2 rounded-md focus:outline-none"></textarea>


                <input type="submit" value="Add Task"
                    class="block bg-blue-400 mt-5 py-2 w-24 rounded-md cursor-pointer" />


            </form>
        </div>

        <div class="flex flex-wrap flex-5 ml-8">

            <% if( files.length> 0){ %>
                <%files.forEach(function(val){%>
                    <div class="w-1/5 bg-zinc-700 p-4 m-6 rounded-md text-gray-50">
                        <h1 class="text-xl font-medium mb-2"><% const str = val.split('_').join(' ')%>
                            <%=`${str.charAt(0).toUpperCase()+ str.slice(1)}` %></h1>

                        <a class="text-blue-500 cursor-pointer">Details..</a>
                    </div>
                    <%})%>
                        <% } else { %>
                            <h3 class="ml-6 text-zinc-500">No Task Found....</h3>
                            <% }%>
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>

</html>